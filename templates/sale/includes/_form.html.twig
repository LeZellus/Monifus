{{ form_start(form, {'attr': {'class': 'grid lg:gap-6 gap-4'}}) }}



<div class="grid lg:grid-cols-3 gap-4 lg:gap-6">
    <div>
        {{ form_label(form.resource, 'Choisissez la ressource ', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.resource) }}
    </div>

    <div>
        {{ form_label(form.buyPrice, 'Prix d\'achat', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.buyPrice) }}
    </div>

    <div>
        {{ form_label(form.sellPrice, 'Prix de vente', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.sellPrice) }}
    </div>

    <div>
        {{ form_label(form.buyDate, 'Date d\'achat', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.buyDate) }}
    </div>

    <div>
        {{ form_label(form.sellDate, 'Date de vente', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.sellDate) }}
    </div>

    <div>
        {{ form_label(form.isSell, 'Vendu', {'label_attr': {'class': 'form-label'}}) }}
        {{ form_widget(form.isSell) }}
    </div>

    <div class="flex justify-center">
        <button class="form-submit">{{ button_label|default('Créer') }}</button>
    </div>

</div>
{{ form_end(form) }}

{% extends 'base.html.twig' %}

{% block title %}Monitor | {{ parent() }}{% endblock %}

{% block body %}

    {% for monitor in monitors %}
        <div class="wrapper-style">
            <div class="flex border-b-2 border-gray-300 dark:border-gray-700 pb-2">
                <img class="h-8 mr-2" src="{{ asset('uploads/pepite.png') }}" alt="Image pépite">
                <h3 class="text-2xl uppercase font-black">{{ monitor.name }}</h3>
            </div>
            <div class="pt-2">
                <div class="flex items-center">
                    <p>Lot de <span class="text-primary-500 font-semibold"></span> :</p>
                    <div class="flex items-center pl-2">
                        <span class="font-bold text-sm">2 450</span>
                        <img class="ml-1 w-3 h-3" src="{{ asset('uploads/kamas.webp') }}" alt="Image kamas">
                    </div>
                </div>
                <div class="flex items-center">
                    <p>Lot de <span class="text-primary-500 font-semibold"></span> :</p>
                    <div class="flex items-center pl-2">
                        <span class="font-bold text-sm">24 500</span>
                        <img class="ml-1 w-3 h-3" src="{{ asset('uploads/kamas.webp') }}" alt="Image kamas">
                    </div>
                </div>
                <div class="flex items-center">
                    <p>Lot de <span class="text-primary-500 font-semibold"></span> :</p>
                    <div class="flex items-center pl-2">
                        <span class="font-bold text-sm">245 000</span>
                        <img class="ml-1 w-3 h-3" src="{{ asset('uploads/kamas.webp') }}" alt="Image kamas">
                    </div>
                </div>
            </div>
        </div>
    {% endfor %}
        <a class="col-start-1 form-submit" href="{{ path('app_monitor_new') }}">Créer un moniteur</a>
    </section>

{% endblock %}

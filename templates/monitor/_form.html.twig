{{ form_start(form, {'attr': {'class': 'grid md:grid-cols-2 lg:gap-6 gap-4'}}) }}
    {{ form_widget(form.price) }}
    {{ form_widget(form.resource) }}
    {{ form_widget(form.stock) }}
    <button class="form-submit">{{ button_label|default('Créer') }}</button>
{{ form_end(form) }}

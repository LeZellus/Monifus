{{ form_start(form, {'attr': {'class': 'form-wrapper'}}) }}
<div class="grid lg:grid-cols-3 gap-4 lg:gap-6">
    <div class="lg:col-span-2">
        {{ form_row(form.resource) }}
    </div>
    {{ form_row(form.pricePer1) }}
    {{ form_row(form.pricePer10) }}
    {{ form_row(form.pricePer100) }}
    {{ form_widget(form.submit) }}
</div>
{{ form_end(form) }}

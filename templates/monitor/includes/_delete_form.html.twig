<form method="post" action="{{ path('app_monitor_delete', {'id': monitor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ monitor.id) }}">
    <button class="btn">Delete</button>
</form>

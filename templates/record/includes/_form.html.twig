{{ form_start(form, {'attr': {'class': 'form-wrapper'}}) }}
<div class="grid lg:grid-cols-2 gap-4 lg:gap-6">
    <div class="lg:col-span-2">
        {{ form_row(form.monster) }}
    </div>
    {{ form_row(form.time) }}
    {{ form_row(form.videoLink) }}
    <div class="lg:col-span-2">
        {{ form_widget(form.submit) }}
    </div>
</div>
{{ form_end(form) }}

